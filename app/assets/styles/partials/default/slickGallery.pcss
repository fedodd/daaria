.slickHolder {
  position: relative;
  margin-top: 50px;
  margin-bottom: 100px;
  padding: 0 20px;
}

.slickGallery {
  position: relative;
  width: calc(100%-40px);
  margin: 0 auto;
}

.slickGallery-item {
  width: 90vw;
}
