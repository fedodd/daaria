@define-mixin respond-to $media {
  @if $media == medium {
    @media only screen and (min-width: 1024px) { @mixin-content; }
  } @else if $media == large {
    @media only screen and (min-width: 1280px) { @mixin-content; }
  }
}
